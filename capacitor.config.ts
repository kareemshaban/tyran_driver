import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apps.dev.tyran.driver',
  appName: 'tyran-driver',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
